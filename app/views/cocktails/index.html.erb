<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h1>Venez boire ces bonnes mixtures </h1>

      <%= link_to 'New cocktail', new_cocktail_path, class: 'btn btn-success' %>

      <ul>
        <% @cocktails.each do |cocktail| %>
          <li><%= link_to cocktail.name, cocktail_path(cocktail) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
